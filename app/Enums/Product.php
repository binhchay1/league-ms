<?php

namespace App\Enums;

final class Product
{
    const STATUS = [
        'new', 'used',
    ];

    const CONDITION = [
        'new', 'used',
    ];

    const STATUS_POST_NEWS = "pending";
}
